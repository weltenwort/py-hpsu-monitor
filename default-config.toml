[logger.levels]
"" = "WARNING"

[mqtt]
enabled = true
configuration_topic_template = "homeassistant/{platform}/{object_id}/config"
state_topic_template = "homeassistant/{platform}/{object_id}/state"
write_topic_template = "homeassistant/{platform}/{object_id}/set"

[mqtt.broker]
hostname = "localhost"
port = 1883

[mqtt.device]
id = "hpsu-0"
name = "HPSU 0"
manufacturer = "Rotex"
model = "Unknown Model"

[can_bus]
sender_id = 0x680

[can_bus.default_register_configuration]
polling_enabled = true
polling_interval = 60.0
